# Keyboard Tools

- `zig build generate-registry`: regenerate `generated/keyboard_registry.zig`.
- `zig build generate-registry -- --check`: verify the registry is up to date.
- `zig run tools/generate_registry.zig`: direct runner alternative.
